// HTML
// put 3 inputs on the page
// put buttons on the screen
// thisInput = document.getElementById('leftInput').
// thisInput.Value




// Tasks
// button elements, divs, a
// thisElement.innerText or innerHTML
// button.value

// start with console.logging every number/button w/ an event listener
// ^ sanity check yo!
// move into writting number values into the left input

// bugs/concerns:
// is the value a number or a string? prseFloat()
// each time you hit a number button, the input is replaced by, not appended with the new number
// var display = document.getElementById("leftInput")value
// display = display + newValue

// as soon as we hit an operator (+, -, *, /, etc...), we need our "focus" to point to the rightInput
// conditionals that check if leftInput has something if the operatorInput has something...
// undefined, getting NaN, divide by zero.